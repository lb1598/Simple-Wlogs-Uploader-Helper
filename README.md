Select the uploader.exe file
Select your logs folder 
Press upload -- after a short delay it automates selecting the upload window >> choose file window >> selects the file >> confirms >> runs, all in one click
